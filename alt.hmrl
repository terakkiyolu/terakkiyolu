<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>THE LENG - İDDİA EDİYORUZ EN İYİSİYİZ</title>
    <script src="https://accounts.google.com/gsi/client" async defer></script>

     PWA Meta Tags 
    <meta name="theme-color" content="#f8f8f8">
    <link rel="apple-touch-icon" href="/icons/icon-192x192.png">
    <link rel="manifest" href="/manifest.json">

    <style>
        :root {
            --primary-color: #000;
            --secondary-color: #333;
            --text-color: #666;
            --background-color: #f8f8f8;
            --border-radius-sm: 10px;
            --border-radius-md: 25px;
            --border-radius-lg: 60px;
            --transition-speed: 0.3s;
            --shadow-light: 0 2px 10px rgba(0, 0, 0, 0.08);
            --shadow-medium: 0 5px 20px rgba(0, 0, 0, 0.15);
            --shadow-strong: 0 10px 30px rgba(0, 0, 0, 0.2);
            --accent-gradient: linear-gradient(135deg, #667eea, #764ba2);
            --card-border-color: #e4e4e7;
            --card-bg-light: #fafafa;
            --card-bg-gradient-from: #f4f4f5;
            --card-bg-gradient-to: white;
            --card-text-dark: #18181b;
            --card-text-medium: #666;
            --card-accent-color: #ff8c00;
        }

        body.no-animations * {
            transition: none !important;
            animation: none !important;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            -webkit-tap-highlight-color: transparent;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            line-height: 1.6;
            color: var(--secondary-color);
            overflow-x: hidden;
            background-color: var(--background-color);
            scroll-behavior: smooth;
        }

        *:focus {
            outline: none;
            box-shadow: none;
        }

        /* Header Styles */
        .header {
            position: fixed;
            top: 0;
            width: 100%;
            background: transparent;
            backdrop-filter: none;
            z-index: 1000;
            transition: all var(--transition-speed) ease;
        }

        .header.scrolled {
            background: transparent;
            box-shadow: none;
        }

        .nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 1rem 2rem;
            max-width: 1200px;
            margin: 0 auto;
        }

        .logo {
            display: inline-flex;
            align-items: center;
            gap: 0.75rem;
            font-weight: 800;
            font-size: clamp(1.5rem, 3.5vw, 1.8rem);
            color: var(--primary-color);
            letter-spacing: -0.8px;
            text-transform: uppercase;
            background: white;
            padding: 0.75rem 1.5rem;
            border-radius: var(--border-radius-lg);
            box-shadow: var(--shadow-light);
            transition: all var(--transition-speed) ease;
            cursor: pointer;
        }

        .logo:hover {
            transform: translateY(-3px) scale(1.01);
            box-shadow: var(--shadow-medium);
        }

        .logo-image {
            height: clamp(30px, 6vw, 40px);
            width: auto;
            object-fit: contain;
        }

        .nav-right {
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .user-info {
            position: relative;
            display: inline-flex;
            align-items: center;
            gap: 0.6rem;
            padding: 0.6rem 1.2rem;
            background: white;
            border-radius: var(--border-radius-lg);
            font-size: clamp(0.88rem, 2vw, 0.98rem);
            font-weight: 600;
            color: var(--secondary-color);
            cursor: pointer;
            transition: all var(--transition-speed) ease;
            user-select: none;
            box-shadow: var(--shadow-light);
        }

        .user-info:hover {
            background: rgba(255, 255, 255, 0.8);
            transform: translateY(-2px) scale(1.02);
            box-shadow: var(--shadow-medium);
        }

        .user-avatar {
            width: 32px;
            height: 32px;
            border-radius: 50%;
            background: var(--accent-gradient);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: bold;
            font-size: 0.8rem;
            flex-shrink: 0;
        }

        /* Hero Section */
        .hero {
            min-height: 100vh;
            background: var(--background-color);
            padding: 8rem 2rem 4rem;
            position: relative;
            overflow: hidden;
            display: flex;
            align-items: center;
            text-align: left;
        }

        .hero-content {
            max-width: 1200px;
            margin: 0 auto;
            position: relative;
            z-index: 2;
            width: 100%;
            text-align: left;
        }

        .gradient-blob {
            position: absolute;
            right: -10%;
            top: 10%;
            width: 500px;
            height: 500px;
            background: linear-gradient(135deg, #ff6b6b, #feca57, #48dbfb);
            border-radius: 50%;
            opacity: 0.7;
            filter: blur(80px);
            animation: float 8s ease-in-out infinite;
        }

        .gradient-blob-2 {
            position: absolute;
            left: -15%;
            bottom: 20%;
            width: 300px;
            height: 300px;
            background: linear-gradient(135deg, #a8edea, #fed6e3);
            border-radius: 50%;
            opacity: 0.5;
            filter: blur(60px);
            animation: float 6s ease-in-out infinite reverse;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0px) rotate(0deg) scale(1); }
            33% { transform: translateY(-30px) rotate(120deg) scale(1.1); }
            66% { transform: translateY(20px) rotate(240deg) scale(0.9); }
        }

        .hero-title {
            font-size: clamp(4rem, 12vw, 8rem);
            font-weight: 300;
            line-height: 0.9;
            margin-bottom: 3rem;
            letter-spacing: -3px;
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color), var(--primary-color));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            position: relative;
            overflow: hidden;
        }

        .hero-title::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.5), transparent);
            animation: shimmer 2.5s infinite linear;
        }

        .hero-subtitle {
            font-size: clamp(1.2rem, 4vw, 1.8rem);
            color: var(--text-color);
            margin-bottom: 4rem;
            max-width: 700px;
            line-height: 1.4;
            position: relative;
            overflow: hidden;
        }

        .hero-subtitle::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
            animation: shimmer 2.5s infinite linear 0.5s;
        }

        @keyframes shimmer {
            0% { left: -100%; }
            100% { left: 100%; }
        }

        .hero-actions {
            display: flex;
            flex-wrap: wrap;
            gap: 2rem;
            align-items: center;
        }

        .cta-button {
            display: inline-flex;
            align-items: center;
            gap: 1rem;
            padding: 1.5rem 3rem;
            background: var(--primary-color);
            border: 3px solid var(--primary-color);
            border-radius: var(--border-radius-lg);
            font-weight: 700;
            text-decoration: none;
            color: white;
            transition: all var(--transition-speed) ease;
            position: relative;
            overflow: hidden;
            font-size: clamp(1rem, 3vw, 1.2rem);
            min-height: 70px;
        }

        .cta-button:hover {
            background: var(--secondary-color);
            border-color: var(--secondary-color);
            transform: translateY(-5px);
            box-shadow: var(--shadow-medium);
        }

        .cta-button.secondary {
            background: transparent;
            color: var(--primary-color);
            border-color: var(--primary-color);
        }

        .cta-button.secondary:hover {
            background: var(--primary-color);
            color: white;
        }

        .cta-button svg {
            stroke: currentColor;
        }

        /* Company Cards Section */
        .company-cards-section {
            padding: 6rem 2rem;
            background-color: var(--background-color);
            text-align: center;
            position: relative;
            z-index: 1;
        }

        .company-cards-section h2 {
            font-size: clamp(2.5rem, 6vw, 4rem);
            font-weight: 800;
            color: var(--primary-color);
            margin-bottom: 4rem;
            letter-spacing: -2px;
        }

        .company-cards-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 1.5rem;
            max-width: 1200px;
            margin: 0 auto;
            justify-items: center;
        }

        /* Card Flip Styles */
        .card-flip-container {
            position: relative;
            width: 100%;
            max-width: 320px;
            height: 360px;
            perspective: 2000px;
            cursor: pointer;
        }

        .card-flip-inner {
            position: relative;
            width: 100%;
            height: 100%;
            transform-style: preserve-3d;
            transition: transform 0.7s cubic-bezier(0.34, 1.56, 0.64, 1);
        }

        .card-flip-container.flipped .card-flip-inner {
            transform: rotateY(180deg);
        }

        .card-flip-front,
        .card-flip-back {
            position: absolute;
            inset: 0;
            width: 100%;
            height: 100%;
            backface-visibility: hidden;
            overflow: hidden;
            border-radius: 16px;
            border: 1px solid var(--card-border-color);
            box-shadow: 0 1px 2px 0 rgba(0,0,0,0.05);
            transition: all 0.7s;
        }

        .card-flip-front {
            transform: rotateY(0deg);
            background: var(--card-bg-light);
            background: linear-gradient(to bottom, var(--card-bg-gradient-from), var(--card-bg-gradient-to));
        }

        .card-flip-container.flipped .card-flip-front {
            opacity: 0;
        }

        .card-flip-back {
            transform: rotateY(180deg);
            padding: 24px;
            background: var(--card-bg-light);
            background: linear-gradient(to bottom, var(--card-bg-gradient-from), var(--card-bg-gradient-to));
            display: flex;
            flex-direction: column;
        }

        .card-flip-container.flipped .card-flip-back {
            opacity: 1;
        }

        /* Front Card Content */
        .card-front-content {
            position: relative;
            height: 100%;
            overflow: hidden;
            display: flex;
            flex-direction: column;
            justify-content: flex-end;
            padding: 20px;
        }

        .card-front-logo {
            position: absolute;
            top: 24px;
            left: 50%;
            transform: translateX(-50%);
            width: 100px;
            height: 100px;
            object-fit: contain;
            filter: grayscale(100%);
            opacity: 0.7;
            transition: filter 0.3s, opacity 0.3s;
        }

        .card-flip-container:hover .card-front-logo {
            filter: grayscale(0%);
            opacity: 1;
        }

        .card-front-text {
            text-align: center;
            margin-top: auto;
        }

        .card-front-title {
            font-size: 1.125rem;
            font-weight: 600;
            color: var(--card-text-dark);
            line-height: 1.375;
            letter-spacing: -0.03em;
            transition: transform 0.5s ease-out;
        }

        .card-flip-container:hover .card-front-title {
            transform: translateY(-4px);
        }

        .card-front-subtitle {
            font-size: 0.875rem;
            color: var(--card-text-medium);
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
            letter-spacing: -0.02em;
            transition: transform 0.5s ease-out 50ms;
        }

        .card-flip-container:hover .card-front-subtitle {
            transform: translateY(-4px);
        }

        .card-front-icon-wrapper {
            position: relative;
            margin-top: 1rem;
            display: flex;
            justify-content: center;
        }

        .card-front-icon {
            position: relative;
            z-index: 10;
            width: 16px;
            height: 16px;
            color: var(--card-accent-color);
            transition: transform 0.3s, rotate 0.3s;
        }

        .card-flip-container:hover .card-front-icon {
            transform: scale(1.1) rotate(-12deg);
        }

        /* Back Card Content */
        .card-back-content {
            flex: 1;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }

        .card-back-text-section {
            margin-bottom: 1.5rem;
        }

        .card-back-title {
            font-size: 1.125rem;
            font-weight: 600;
            color: var(--card-text-dark);
            line-height: 1.375;
            letter-spacing: -0.02em;
            transition: transform 0.5s ease-out;
        }

        .card-flip-container.flipped .card-back-title {
            transform: translateY(-2px);
        }

        .card-back-description {
            font-size: 0.875rem;
            color: var(--card-text-medium);
            letter-spacing: -0.02em;
            line-height: 1.5;
            margin-top: 0.5rem;
            transition: transform 0.5s ease-out;
        }

        .card-flip-container.flipped .card-back-description {
            transform: translateY(-2px);
        }

        .card-back-features {
            margin-top: 1rem;
        }

        .card-back-feature-item {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 0.875rem;
            color: var(--card-text-medium);
            transition: all 0.5s;
            margin-bottom: 0.5rem;
        }

        .card-back-feature-icon {
            width: 12px;
            height: 12px;
            color: var(--card-accent-color);
        }

        /* Enhanced Contact Panel Styles */
        .contact-panel-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.8);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 2000;
            opacity: 0;
            visibility: hidden;
            transition: opacity 0.5s ease, visibility 0.5s ease;
            backdrop-filter: blur(10px);
        }

        .contact-panel-overlay.active {
            opacity: 1;
            visibility: visible;
        }

        .contact-panel {
            background: linear-gradient(135deg, #ffffff 0%, #f8f9fa 100%);
            padding: 3rem;
            border-radius: 20px;
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.25);
            max-width: 500px;
            width: 90%;
            text-align: left;
            transform: scale(0.7) translateY(100px) rotateX(15deg);
            opacity: 0;
            transition: all 0.6s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            position: relative;
            overflow: hidden;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .contact-panel::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.4), transparent);
            animation: panel-shimmer 3s infinite;
        }

        @keyframes panel-shimmer {
            0% { left: -100%; }
            100% { left: 100%; }
        }

        .contact-panel-overlay.active .contact-panel {
            transform: scale(1) translateY(0) rotateX(0deg);
            opacity: 1;
        }

        .contact-panel-icon {
            width: 80px;
            height: 80px;
            color: #667eea;
            margin-bottom: 2rem;
            animation: icon-pulse 2s infinite ease-in-out;
            filter: drop-shadow(0 4px 8px rgba(102, 126, 234, 0.3));
        }

        @keyframes icon-pulse {
            0%, 100% { 
                transform: scale(1) rotate(0deg); 
                filter: drop-shadow(0 4px 8px rgba(102, 126, 234, 0.3));
            }
            50% { 
                transform: scale(1.1) rotate(5deg); 
                filter: drop-shadow(0 8px 16px rgba(102, 126, 234, 0.5));
            }
        }

        .contact-panel h2 {
            font-size: clamp(2rem, 5vw, 2.8rem);
            margin-bottom: 1.5rem;
            color: var(--primary-color);
            letter-spacing: -1px;
            font-weight: 700;
            text-align: left;
            background: linear-gradient(135deg, #000, #333);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .contact-panel p {
            font-size: clamp(1.1rem, 3vw, 1.2rem);
            margin-bottom: 2.5rem;
            color: var(--secondary-color);
            line-height: 1.6;
            text-align: left;
        }

        .contact-panel .contact-info {
            margin-top: 2rem;
            display: flex;
            flex-direction: column;
            gap: 1.2rem;
        }

        .contact-panel .contact-item {
            display: flex;
            align-items: center;
            justify-content: flex-start;
            gap: 1rem;
            font-size: clamp(1rem, 2.8vw, 1.1rem);
            color: var(--secondary-color);
            text-decoration: none;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            padding: 1.2rem 1.5rem;
            border-radius: 15px;
            background: rgba(255, 255, 255, 0.7);
            position: relative;
            overflow: hidden;
            border: 1px solid rgba(0, 0, 0, 0.05);
            transform: translateX(-20px);
            opacity: 0;
            animation: slide-in-left 0.6s ease-out forwards;
        }

        .contact-panel .contact-item:nth-child(1) { animation-delay: 0.1s; }
        .contact-panel .contact-item:nth-child(2) { animation-delay: 0.2s; }
        .contact-panel .contact-item:nth-child(3) { animation-delay: 0.3s; }
        .contact-panel .contact-item:nth-child(4) { animation-delay: 0.4s; }

        @keyframes slide-in-left {
            to {
                transform: translateX(0);
                opacity: 1;
            }
        }

        .contact-panel .contact-item::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(102, 126, 234, 0.1), transparent);
            transition: left 0.5s ease;
        }

        .contact-panel .contact-item:hover::before {
            left: 100%;
        }

        .contact-panel .contact-item:hover {
            color: white;
            background: linear-gradient(135deg, #667eea, #764ba2);
            transform: translateX(10px) scale(1.02);
            box-shadow: 0 10px 25px rgba(102, 126, 234, 0.3);
            border-color: transparent;
        }

        .contact-panel .contact-item svg {
            width: clamp(22px, 4.5vw, 26px);
            height: clamp(22px, 4.5vw, 26px);
            stroke: currentColor;
            stroke-width: 2;
            transition: transform 0.3s ease;
        }

        .contact-panel .contact-item:hover svg {
            transform: scale(1.2) rotate(10deg);
        }

        .contact-panel .close-button {
            position: absolute;
            top: 1.5rem;
            right: 1.5rem;
            background: rgba(0, 0, 0, 0.05);
            border: none;
            font-size: 2rem;
            cursor: pointer;
            color: var(--secondary-color);
            transition: all 0.3s ease;
            padding: 0.5rem;
            border-radius: 50%;
            line-height: 1;
            width: 45px;
            height: 45px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .contact-panel .close-button:hover {
            color: white;
            background: #ff4757;
            transform: rotate(90deg) scale(1.1);
            box-shadow: 0 5px 15px rgba(255, 71, 87, 0.4);
        }

        /* Loading Animation */
        .loading {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: var(--background-color);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 9999;
            transition: opacity 0.5s ease;
        }

        .loading.hidden {
            opacity: 0;
            pointer-events: none;
        }

        .spinner {
            width: 50px;
            height: 50px;
            border: 4px solid #f3f3f3;
            border-top: 4px solid var(--primary-color);
            border-radius: 50%;
            animation: spin 1s linear infinite;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        /* Ripple Effect */
        .ripple {
            position: absolute;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.7);
            animation: ripple-animation 0.6s linear forwards;
            transform: scale(0);
            pointer-events: none;
            z-index: 1;
        }

        .cta-button.secondary .ripple,
        .contact-panel .contact-item .ripple,
        .profile-drawer-item .ripple,
        .settings-panel .settings-option button .ripple {
            background: rgba(0, 0, 0, 0.1);
        }

        @keyframes ripple-animation {
            to {
                transform: scale(2.5);
                opacity: 0;
            }
        }

        /* Scroll Animation */
        .scroll-animate {
            opacity: 0;
            transform: translateY(20px);
            transition: opacity 0.6s ease-out, transform 0.6s ease-out;
        }

        .scroll-animate.visible {
            opacity: 1;
            transform: translateY(0);
        }

        /* Profile Drawer Styles */
        .profile-drawer-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.6);
            display: flex;
            justify-content: flex-end;
            align-items: flex-end;
            z-index: 2000;
            opacity: 0;
            visibility: hidden;
            transition: opacity var(--transition-speed) ease, visibility var(--transition-speed) ease;
        }

        .profile-drawer-overlay.active {
            opacity: 1;
            visibility: visible;
        }

        .profile-drawer {
            background: white;
            width: clamp(280px, 80vw, 380px);
            height: auto;
            max-height: 90vh;
            border-top-left-radius: var(--border-radius-md);
            border-top-right-radius: var(--border-radius-md);
            box-shadow: var(--shadow-medium);
            transform: translateY(100%);
            opacity: 0;
            transition: transform 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94), opacity 0.4s ease-out;
            position: relative;
            display: flex;
            flex-direction: column;
            padding: 1.5rem;
        }

        .profile-drawer-overlay.active .profile-drawer {
            transform: translateY(0);
            opacity: 1;
        }

        .profile-drawer-header {
            padding-bottom: 1rem;
            border-bottom: 1px solid rgba(0, 0, 0, 0.05);
            margin-bottom: 1.5rem;
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .profile-drawer-header .user-avatar {
            width: 50px;
            height: 50px;
            font-size: 1.2rem;
        }

        .profile-drawer-title {
            font-size: clamp(1.4rem, 4vw, 1.8rem);
            font-weight: bold;
            color: var(--primary-color);
        }

        .profile-drawer-description {
            font-size: clamp(0.85rem, 2.5vw, 0.95rem);
            color: var(--text-color);
            margin-top: 0.5rem;
        }

        .profile-drawer-content {
            flex-grow: 1;
            overflow-y: auto;
            padding-right: 0.5rem;
        }

        .profile-drawer-item {
            display: flex;
            align-items: center;
            gap: 1rem;
            padding: 1rem;
            border-radius: var(--border-radius-sm);
            color: var(--secondary-color);
            text-decoration: none;
            transition: background var(--transition-speed) ease;
            cursor: pointer;
            position: relative;
            overflow: hidden;
            margin-bottom: 0.5rem;
        }

        .profile-drawer-item:hover {
            background: rgba(0, 0, 0, 0.05);
        }

        .profile-drawer-item svg {
            width: 22px;
            height: 22px;
            stroke: currentColor;
            stroke-width: 2;
            flex-shrink: 0;
        }

        .profile-drawer-item span {
            font-size: clamp(0.95rem, 2.8vw, 1.05rem);
            font-weight: 500;
        }

        .profile-drawer-footer {
            padding-top: 1.5rem;
            border-top: 1px solid rgba(0, 0, 0, 0.05);
            margin-top: 1.5rem;
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        .profile-drawer-close-button {
            background: var(--primary-color);
            color: white;
            border: none;
            padding: 0.8rem 1.5rem;
            border-radius: var(--border-radius-md);
            font-weight: 600;
            cursor: pointer;
            transition: all var(--transition-speed) ease;
            font-size: clamp(0.9rem, 2.5vw, 1rem);
            position: relative;
            overflow: hidden;
        }

        .profile-drawer-close-button:hover {
            background: var(--secondary-color);
            transform: translateY(-2px);
            box-shadow: var(--shadow-light);
        }

        /* Settings Panel Styles */
        .settings-panel-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.6);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 2000;
            opacity: 0;
            visibility: hidden;
            transition: opacity 0.5s ease, visibility 0.5s ease;
            backdrop-filter: blur(10px);
        }

        .settings-panel-overlay.active {
            opacity: 1;
            visibility: visible;
        }

        .settings-panel {
            background: linear-gradient(135deg, #ffffff 0%, #f8f9fa 100%);
            padding: 2.5rem;
            border-radius: 20px;
            box-shadow: 0 25px 50px rgba(0, 0, 0, 0.25);
            max-width: 600px;
            width: 90%;
            text-align: left;
            transform: scale(0.7) translateY(100px) rotateX(15deg);
            opacity: 0;
            transition: all 0.6s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            position: relative;
            overflow: hidden;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .settings-panel-overlay.active .settings-panel {
            transform: scale(1) translateY(0) rotateX(0deg);
            opacity: 1;
        }

        .settings-panel .close-button {
            position: absolute;
            top: 1.5rem;
            right: 1.5rem;
            background: rgba(0, 0, 0, 0.05);
            border: none;
            font-size: 2rem;
            cursor: pointer;
            color: var(--secondary-color);
            transition: all 0.3s ease;
            padding: 0.5rem;
            border-radius: 50%;
            line-height: 1;
            width: 45px;
            height: 45px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .settings-panel .close-button:hover {
            color: white;
            background: #ff4757;
            transform: rotate(90deg) scale(1.1);
            box-shadow: 0 5px 15px rgba(255, 71, 87, 0.4);
        }

        .settings-panel h2 {
            font-size: clamp(2rem, 5vw, 2.8rem);
            margin-bottom: 1.5rem;
            color: var(--primary-color);
            letter-spacing: -1px;
            font-weight: 700;
            text-align: left;
            background: linear-gradient(135deg, #000, #333);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .settings-group {
            margin-bottom: 2rem;
            padding-bottom: 1.5rem;
            border-bottom: 1px solid rgba(0, 0, 0, 0.05);
        }

        .settings-group:last-child {
            border-bottom: none;
            margin-bottom: 0;
            padding-bottom: 0;
        }

        .settings-group h3 {
            font-size: 1.4rem;
            color: var(--primary-color);
            margin-bottom: 1rem;
        }

        .settings-option {
            display: flex;
            align-items: center;
            justify-content: space-between;
            margin-bottom: 1rem;
            padding: 0.8rem 0;
        }

        .settings-option label {
            font-size: 1rem;
            color: var(--secondary-color);
            font-weight: 500;
        }

        .settings-option .theme-buttons {
            display: flex;
            gap: 0.5rem;
        }

        .settings-option .theme-buttons button {
            padding: 0.6rem 1.2rem;
            border: 1px solid var(--card-border-color);
            border-radius: var(--border-radius-sm);
            background: var(--card-bg-light);
            color: var(--card-text-dark);
            cursor: pointer;
            transition: all 0.2s ease;
            font-weight: 500;
            position: relative;
            overflow: hidden;
        }

        .settings-option .theme-buttons button.active,
        .settings-option .theme-buttons button:hover {
            background: var(--primary-color);
            color: white;
            border-color: var(--primary-color);
            box-shadow: var(--shadow-light);
        }

        .settings-option .toggle-switch {
            position: relative;
            display: inline-block;
            width: 40px;
            height: 24px;
        }

        .settings-option .toggle-switch input {
            opacity: 0;
            width: 0;
            height: 0;
        }

        .settings-option .slider {
            position: absolute;
            cursor: pointer;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-color: #ccc;
            transition: .4s;
            border-radius: 24px;
        }

        .settings-option .slider:before {
            position: absolute;
            content: "";
            height: 16px;
            width: 16px;
            left: 4px;
            bottom: 4px;
            background-color: white;
            transition: .4s;
            border-radius: 50%;
        }

        .settings-option input:checked + .slider {
            background-color: #667eea;
        }

        .settings-option input:checked + .slider:before {
            transform: translateX(16px);
        }

        /* Mobile Responsive Adjustments */
        @media (max-width: 768px) {
            .nav {
                padding: 0.75rem 1.25rem;
            }

            .nav-right {
                gap: 0.5rem;
            }

            .hero {
                padding: 7rem 1.25rem 3rem;
            }

            .hero-actions {
                flex-direction: column;
                align-items: flex-start;
                width: 100%;
            }

            .cta-button {
                width: 100%;
                max-width: none;
            }

            /* Mobile cards: 2 per row */
            .company-cards-grid {
                grid-template-columns: repeat(2, 1fr);
                gap: 1rem;
                max-width: 100%;
            }

            .card-flip-container {
                max-width: 100%;
                height: 300px;
            }

            .profile-drawer {
                width: 100%;
                border-bottom-left-radius: 0;
                border-bottom-right-radius: 0;
            }

            .contact-panel, .settings-panel {
                padding: 2rem;
                margin: 1rem;
            }

            .contact-panel h2, .settings-panel h2 {
                font-size: 2rem;
            }

            .settings-option {
                flex-direction: column;
                align-items: flex-start;
                gap: 0.5rem;
            }

            .settings-option .theme-buttons {
                width: 100%;
                justify-content: space-around;
            }

            .settings-option .theme-buttons button {
                flex: 1;
            }
        }

        @media (max-width: 480px) {
            .nav {
                padding: 0.5rem 1rem;
            }

            .hero {
                padding: 6rem 1rem 2rem;
            }

            .company-cards-grid {
                gap: 0.8rem;
            }

            .card-flip-container {
                height: 280px;
            }

            .contact-panel, .settings-panel {
                padding: 1.5rem;
            }
        }

        /* Very small screens - single column if needed */
        @media (max-width: 360px) {
            .company-cards-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
     Loading Screen 
    <div class="loading" id="loading">
        <div class="spinner"></div>
    </div>

     Header 
    <header class="header" id="header">
        <nav class="nav">
            <div class="logo">
                <img src="/images/the1.png" alt="THE LENG Logo" class="logo-image">
                THE LENG
            </div>
            <div class="nav-right">
                <div id="userInfoElement" class="user-info" style="display: none;">
                    <div class="user-avatar" id="userAvatar"></div>
                    <span id="userName"></span>
                </div>
            </div>
        </nav>
    </header>

     Hero Section 
    <section class="hero" id="home">
        <div class="gradient-blob"></div>
        <div class="gradient-blob-2"></div>
        <div class="hero-content scroll-animate" id="heroContent">
            <h1 class="hero-title">
                İDDİA EDİYORUZ<br>
                EN İYİSİYİZ.
            </h1>
            <p class="hero-subtitle">
                Dijital dünyada fark yaratan, sınırları zorlayan ve her projede mükemmelliği hedefleyen 
                teknoloji şirketi. Hayallerinizi gerçeğe dönüştürüyoruz.
            </p>
            <div class="hero-actions">
                <button id="googleSignInButton" class="cta-button google-signin">
                    <svg width="24" height="24" viewBox="0 0 24 24" fill="currentColor">
                        <path d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                        <path d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                        <path d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z"/>
                        <path d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"/>
                    </svg>
                    Google ile Giriş Yap
                </button>
            </div>
        </div>
    </section>

     Company Cards Section 
    <section class="company-cards-section" id="companies">
        <h2 class="scroll-animate">Bizi Tercih Eden Şirketler</h2>
        <div class="company-cards-grid" id="companyCardsGrid">
             Cards will be dynamically inserted here 
        </div>
    </section>

     Enhanced Contact Panel 
    <div class="contact-panel-overlay" id="contactPanelOverlay">
        <div class="contact-panel">
            <button class="close-button" id="closeContactPanel">&times;</button>
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-headphones contact-panel-icon"><path d="M3 14h3a2 2 0 0 1 2 2v3a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-7A9 9 0 0 1 21 12V7a2 2 0 0 0-2-2h-3a2 2 0 0 0-2 2v5a2 2 0 0 1-2 2h-1.5"/></svg>
            <h2>Müşteri Danışmanına Ulaşın</h2>
            <p>Size yardımcı olmaktan mutluluk duyarız. Aşağıdaki iletişim kanallarından bize ulaşabilirsiniz:</p>
            <div class="contact-info">
                <a href="tel:+905551234567" class="contact-item">
                    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-phone"><path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.63A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/></svg>
                    +90 555 123 45 67
                </a>
                <a href="mailto:info@theleng.com" class="contact-item">
                    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-mail"><rect width="20" height="16" x="2" y="4" rx="2"/><path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"/></svg>
                    info@theleng.com
                </a>
                <a href="https://wa.me/905551234567" target="_blank" rel="noopener noreferrer" class="contact-item">
                    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-message-circle"><path d="M7.9 20A9 9 0 1 0 4 16.1L2 22Z"/></svg>
                    WhatsApp
                </a>
                <a href="https://twitter.com/theleng" target="_blank" rel="noopener noreferrer" class="contact-item">
                    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-twitter"><path d="M22 4s-.7 2.1-2 3.4c1.6 10-9.4 17-18 11.6 2.2.1 4.4-.6 6-2 1.1-.9 1.8-2.2 2.1-3.7C3.2 15.2 2 13 2 12c2.1 1.2 4.4 1.8 6.7 1.7-1.1-3.9 1.5-7.5 5.4-8.5 3.6-.8 7.1 2 7.1 2z"/></svg>
                    Twitter
                </a>
            </div>
        </div>
    </div>

     Profile Drawer Overlay 
    <div class="profile-drawer-overlay" id="profileDrawerOverlay">
        <div class="profile-drawer" id="profileDrawer">
            <div class="profile-drawer-header">
                <div class="user-avatar" id="drawerUserAvatar"></div>
                <div>
                    <h3 class="profile-drawer-title" id="drawerUserName"></h3>
                    <p class="profile-drawer-description" id="drawerUserEmail"></p>
                </div>
            </div>
            <div class="profile-drawer-content">
                <a href="#" class="profile-drawer-item" id="drawerPanelLink" style="display: none;" target="_blank" rel="noopener noreferrer">
                    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-layout-dashboard"><rect width="7" height="9" x="3" y="3" rx="1"/><rect width="7" height="5" x="14" y="3" rx="1"/><rect width="7" height="9" x="14" y="12" rx="1"/><rect width="7" height="5" x="3" y="16" rx="1"/></svg>
                    <span>Panele Yönlendir</span>
                </a>
                <a href="#" class="profile-drawer-item" id="drawerMakePayment">
                    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-credit-card"><rect width="20" height="14" x="2" y="5" rx="2"/><line x1="2" x2="22" y1="10" y2="10"/></svg>
                    <span>Ödeme Yap</span>
                </a>
                <a href="#" class="profile-drawer-item" id="drawerEditProfile">
                    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-user-cog"><circle cx="18" cy="15" r="3"/><path d="M6 21v-2a4 4 0 0 1 4-4h4"/><path d="M19.07 4.93a10 10 0 0 0-16.28 6.86"/><path d="M22 17v.01"/><path d="M22 20v.01"/><path d="M22 14v.01"/><path d="M15 22v.01"/><path d="M18 22v.01"/><path d="M21 22v.01"/></svg>
                    <span>Profili Düzenle</span>
                </a>
                <a href="#" class="profile-drawer-item" id="drawerSettings">
                    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="lucide lucide-settings"><path d="M12.22 2h-.44a2 2 0 0 0-2 2v.18a2 2 0 0 1-1 1.73l-.43.25a2 2 0 0 1-2 0l-.15-.08a2 2 0 0 0-2.73.73l-.78 1.35a2 2 0 0 0 .73 2.73l.04.02a2 2 0 0 1 .97 1.95v.44a2 2 0 0 1-.97 1.95l-.04.02a2 2 0 0 0-.73 2.73l.78 1.35a2 2 0 0 0 2.73.73l.15-.08a2 2 0 0 1 2 0l.43.25a2 2 0 0 1 1 1.73V20a2 2 0 0 0 2 2h.44a2 2 0 0 0 2-2v-.18a2 2 0 0 1 1-1.73l.43-.25a2 2 0 0 1 2 0l.15.08a2 2 0 0 0 2.73-.73l.78-1.35a2 2 0 0 0-.73-2.73l-.04-.02a2 2 0 0 1-.97-1.95v-.44a2 2 0 0 1 .97-1.95l.04-.02a2 2 0 0 0 .73-2.73l-.78-1.35a2 2 0 0 0-2.73-.73l-.15.08a2 2 0 0 1-2 0l-.43-.25a2 2 0 0 1-1-1.73V4a2 2 0 0 0-2-2z"/><circle cx="12" cy="12" r="3"/></svg>
                    <span>Ayarlar</span>
                </a>
            </div>
            <div class="profile-drawer-footer">
                <button class="profile-drawer-close-button" id="drawerSignOutButton">
                    Çıkış Yap
                </button>
            </div>
        </div>
    </div>

     Settings Panel Overlay 
    <div class="settings-panel-overlay" id="settingsPanelOverlay">
        <div class="settings-panel">
            <button class="close-button" id="closeSettingsPanel">&times;</button>
            <h2>Ayarlar</h2>

            <div class="settings-group">
                <h3>Tema Seçimi</h3>
                <div class="settings-option">
                    <label>Site Teması:</label>
                    <div class="theme-buttons" id="themeButtons">
                        <button data-theme="default">Varsayılan</button>
                        <button data-theme="dark">Koyu</button>
                        <button data-theme="vibrant">Canlı</button>
                    </div>
                </div>
            </div>

            <div class="settings-group">
                <h3>Görsel Ayarlar</h3>
                <div class="settings-option">
                    <label for="animationToggle">Animasyonları Kapat</label>
                    <label class="toggle-switch">
                        <input type="checkbox" id="animationToggle">
                        <span class="slider"></span>
                    </label>
                </div>
            </div>

            <div class="settings-group">
                <h3>Diğer Ayarlar</h3>
                <div class="settings-option">
                    <label>Dil Seçimi:</label>
                    <select id="languageSelect" style="padding: 0.5rem; border-radius: 8px; border: 1px solid var(--card-border-color); background: var(--card-bg-light); color: var(--card-text-dark);">
                        <option value="tr">Türkçe</option>
                        <option value="en">English</option>
                    </select>
                </div>
            </div>
        </div>
    </div>

    <script>
        // PWA Service Worker Registration
        if ('serviceWorker' in navigator) {
            window.addEventListener('load', () => {
                navigator.serviceWorker.register('/service-worker.js')
                    .then(registration => {
                        console.log('Service Worker registered with scope:', registration.scope);
                    })
                    .catch(error => {
                        console.error('Service Worker registration failed:', error);
                    });
            });
        }

        // Loading screen
        window.addEventListener('load', () => {
            setTimeout(() => {
                document.getElementById('loading').classList.add('hidden');
            }, 1000);
        });

        // Header scroll effect
        window.addEventListener('scroll', () => {
            const header = document.getElementById('header');
            if (window.scrollY > 100) {
                header.classList.add('scrolled');
            } else {
                header.classList.remove('scrolled');
            }
        });

        // Google Sign-In Implementation
        let currentUser = null;
        const heroContentDiv = document.getElementById('heroContent');
        
        const googleSignInButton = document.getElementById('googleSignInButton');
        const userInfoElement = document.getElementById('userInfoElement');
        const userName = document.getElementById('userName');
        const userAvatar = document.getElementById('userAvatar');
        
        let contactAdvisorButton = null;

        const userConfigurations = {
            "m.yusufertek@gmail.com": {
                name: "Yusuf Ertek",
                panelLink: "https://terakkiyolu.org/new1",
                welcomeTitle: "HOŞGELDİNİZ Yusuf BEY",
                welcomeSubtitle: "Aşağıdaki butonda panele giriş yapabilirsiniz."
            }
        };

        function initializeGoogleSignIn() {
            if (typeof google !== 'undefined' && google.accounts && google.accounts.id) {
                google.accounts.id.initialize({
                    client_id: '858159418763-lklgnmvm6qa08dehrstd7e6eo8dauq04.apps.googleusercontent.com',
                    callback: handleCredentialResponse,
                    auto_select: false,
                    cancel_on_tap_outside: true
                });
                console.log('Google Sign-In initialized successfully.');
            } else {
                console.warn('Google Sign-In script not yet loaded. Retrying initialization...');
                setTimeout(initializeGoogleSignIn, 500);
            }
        }

        window.addEventListener('load', initializeGoogleSignIn);

        function handleCredentialResponse(response) {
            try {
                const payload = JSON.parse(atob(response.credential.split('.')[1]));
                
                currentUser = {
                    name: payload.name,
                    email: payload.email,
                    picture: payload.picture,
                    given_name: payload.given_name
                };
                console.log('User signed in:', currentUser.email);
                updateUIForSignedInUser();
            } catch (error) {
                console.error('Error decoding JWT or processing credential response:', error);
                alert('Giriş yapılırken bir hata oluştu. Lütfen tekrar deneyin.');
            }
        }

        function updateUIForSignedInUser() {
            googleSignInButton.style.display = 'none';
            userInfoElement.style.display = 'flex';
            
            userName.textContent = currentUser.given_name || currentUser.name;
            
            if (currentUser.picture) {
                userAvatar.innerHTML = `<img src="${currentUser.picture}" alt="Avatar" style="width: 100%; height: 100%; border-radius: 50%; object-fit: cover;">`;
            } else {
                userAvatar.textContent = currentUser.name.charAt(0).toUpperCase();
            }

            userInfoElement.removeEventListener('click', showProfileDrawer);
            userInfoElement.addEventListener('click', showProfileDrawer);

            const userConfig = userConfigurations[currentUser.email];
            if (userConfig) {
                displayPersonalizedContent(userConfig);
            } else {
                displayUnregisteredUserContent();
            }
            ensureContactButtonInHeroActions();
        }

        function displayPersonalizedContent(config) {
            heroContentDiv.innerHTML = `
                <h1 class="hero-title">
                    ${config.welcomeTitle}
                </h1>
                <p class="hero-subtitle">
                    ${config.welcomeSubtitle}
                </p>
                <div class="hero-actions">
                    <a href="${config.panelLink}" class="cta-button" target="_blank" rel="noopener noreferrer">
                        PANELE GİRİŞ
                        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                            <path d="m9 18 6-6-6-6"/>
                        </svg>
                    </a>
                </div>
            `;
            applyShimmerToText();
            ensureContactButtonInHeroActions();
            addRippleEffect(heroContentDiv.querySelector('.cta-button:not(.secondary)'));
        }

        function displayUnregisteredUserContent() {
            heroContentDiv.innerHTML = `
                <h1 class="hero-title">
                    BİZİMLE ÇALIŞMAYA<br>
                    NE DERSİNİZ?
                </h1>
                <p class="hero-subtitle">
                    Siz de dijital dönüşüm yolculuğunuzda bize katılın ve hayallerinizi gerçeğe dönüştürelim.
                </p>
                <div class="hero-actions">
                </div>
            `;
            applyShimmerToText();
            ensureContactButtonInHeroActions();
        }

        function restoreDefaultHeroContent() {
            heroContentDiv.innerHTML = `
                <h1 class="hero-title">
                    İDDİA EDİYORUZ<br>
                    EN İYİSİYİZ.
                </h1>
                <p class="hero-subtitle">
                    Dijital dünyada fark yaratan, sınırları zorlayan ve her projede mükemmelliği hedefleyen 
                    teknoloji şirketi. Hayallerinizi gerçeğe dönüştürüyoruz.
                </p>
                <div class="hero-actions">
                </div>
            `;
            const heroActionsDiv = heroContentDiv.querySelector('.hero-actions');

            if (!currentUser) {
                googleSignInButton.style.display = 'flex';
                heroActionsDiv.appendChild(googleSignInButton);
                addRippleEffect(googleSignInButton);
                removeContactButtonFromHeroActions();
            } else {
                googleSignInButton.style.display = 'none';
                ensureContactButtonInHeroActions();
            }
            
            initializeScrollAnimations();
            applyShimmerToText();
        }

        function signOut() {
            if (confirm('Çıkış yapmak istediğinizden emin misiniz?')) {
                google.accounts.id.disableAutoSelect();
                currentUser = null;
                
                googleSignInButton.style.display = 'flex';
                userInfoElement.style.display = 'none';
                hideProfileDrawer();
                
                removeContactButtonFromHeroActions();
                restoreDefaultHeroContent();

                const toast = document.createElement('div');
                toast.style.cssText = `
                    position: fixed;
                    top: 100px;
                    right: 20px;
                    background: #dc3545;
                    color: white;
                    padding: 1rem 1.5rem;
                    border-radius: 10px;
                    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
                    z-index: 10000;
                    font-weight: 600;
                    transform: translateX(100%);
                    transition: transform 0.3s ease;
                `;
                toast.textContent = 'Başarıyla çıkış yaptınız! 👋';
                
                document.body.appendChild(toast);
                
                setTimeout(() => {
                    toast.style.transform = 'translateX(0)';
                }, 100);
                
                setTimeout(() => {
                    toast.style.transform = 'translateX(100%)';
                    setTimeout(() => {
                        document.body.removeChild(toast);
                    }, 300);
                }, 2000);
            }
        }

        googleSignInButton.addEventListener('click', () => {
            google.accounts.id.prompt();
        });

        // Contact Panel Logic
        const contactPanelOverlay = document.getElementById('contactPanelOverlay');
        const closeContactPanelButton = document.getElementById('closeContactPanel');

        function showContactPanel() {
            contactPanelOverlay.classList.add('active');
        }

        function hideContactPanel() {
            contactPanelOverlay.classList.remove('active');
        }

        closeContactPanelButton.addEventListener('click', hideContactPanel);
        contactPanelOverlay.addEventListener('click', (e) => {
            if (e.target === contactPanelOverlay) {
                hideContactPanel();
            }
        });

        // Profile Drawer Logic
        const profileDrawerOverlay = document.getElementById('profileDrawerOverlay');
        const profileDrawer = document.getElementById('profileDrawer');
        const drawerUserAvatar = document.getElementById('drawerUserAvatar');
        const drawerUserName = document.getElementById('drawerUserName');
        const drawerUserEmail = document.getElementById('drawerUserEmail');
        const drawerPanelLink = document.getElementById('drawerPanelLink');
        const drawerMakePayment = document.getElementById('drawerMakePayment');
        const drawerEditProfile = document.getElementById('drawerEditProfile');
        const drawerSettings = document.getElementById('drawerSettings'); // New
        const drawerSignOutButton = document.getElementById('drawerSignOutButton');

        function showProfileDrawer() {
            if (!currentUser) return;

            if (currentUser.picture) {
                drawerUserAvatar.innerHTML = `<img src="${currentUser.picture}" alt="Avatar" style="width: 100%; height: 100%; border-radius: 50%; object-fit: cover;">`;
            } else {
                drawerUserAvatar.textContent = currentUser.name.charAt(0).toUpperCase();
            }
            drawerUserName.textContent = currentUser.name;
            drawerUserEmail.textContent = currentUser.email;

            const userConfig = userConfigurations[currentUser.email];
            if (userConfig && userConfig.panelLink) {
                drawerPanelLink.href = userConfig.panelLink;
                drawerPanelLink.style.display = 'flex';
            } else {
                drawerPanelLink.style.display = 'none';
            }

            profileDrawerOverlay.classList.add('active');
        }

        function hideProfileDrawer() {
            profileDrawerOverlay.classList.remove('active');
        }

        profileDrawerOverlay.addEventListener('click', (e) => {
            if (e.target === profileDrawerOverlay) {
                hideProfileDrawer();
            }
        });

        drawerMakePayment.addEventListener('click', (e) => {
            e.preventDefault();
            alert('Ödeme sayfası yakında eklenecek!');
            hideProfileDrawer();
        });

        drawerEditProfile.addEventListener('click', (e) => {
            e.preventDefault();
            alert('Profil düzenleme sayfası yakında eklenecek!');
            hideProfileDrawer();
        });

        drawerSignOutButton.addEventListener('click', signOut);

        // Settings Panel Logic
        const settingsPanelOverlay = document.getElementById('settingsPanelOverlay');
        const closeSettingsPanelButton = document.getElementById('closeSettingsPanel');
        const themeButtons = document.getElementById('themeButtons');
        const animationToggle = document.getElementById('animationToggle');
        const languageSelect = document.getElementById('languageSelect');

        const themes = {
            'default': {
                '--primary-color': '#000',
                '--secondary-color': '#333',
                '--text-color': '#666',
                '--background-color': '#f8f8f8',
                '--card-border-color': '#e4e4e7',
                '--card-bg-light': '#fafafa',
                '--card-bg-gradient-from': '#f4f4f5',
                '--card-bg-gradient-to': 'white',
                '--card-text-dark': '#18181b',
                '--card-text-medium': '#666',
                '--card-accent-color': '#ff8c00',
                '--accent-gradient': 'linear-gradient(135deg, #667eea, #764ba2)',
            },
            'dark': {
                '--primary-color': '#f8f8f8',
                '--secondary-color': '#e0e0e0',
                '--text-color': '#b0b0b0',
                '--background-color': '#1a1a1a',
                '--card-border-color': '#333',
                '--card-bg-light': '#2a2a2a',
                '--card-bg-gradient-from': '#222',
                '--card-bg-gradient-to': '#1a1a1a',
                '--card-text-dark': '#f8f8f8',
                '--card-text-medium': '#b0b0b0',
                '--card-accent-color': '#8c9eff',
                '--accent-gradient': 'linear-gradient(135deg, #4a55a2, #2e3a87)',
            },
            'vibrant': {
                '--primary-color': '#4a00e0',
                '--secondary-color': '#6a00f0',
                '--text-color': '#333',
                '--background-color': '#e0f7fa',
                '--card-border-color': '#a7d9f0',
                '--card-bg-light': '#f0f8ff',
                '--card-bg-gradient-from': '#e0f7fa',
                '--card-bg-gradient-to': '#ffffff',
                '--card-text-dark': '#1a1a1a',
                '--card-text-medium': '#555',
                '--card-accent-color': '#ff4081',
                '--accent-gradient': 'linear-gradient(135deg, #ff4081, #81d4fa)',
            }
        };

        function applyTheme(themeName) {
            const root = document.documentElement;
            const selectedTheme = themes[themeName];
            for (const [property, value] of Object.entries(selectedTheme)) {
                root.style.setProperty(property, value);
            }
            localStorage.setItem('selectedTheme', themeName);
            updateThemeButtons(themeName);
        }

        function updateThemeButtons(activeTheme) {
            document.querySelectorAll('#themeButtons button').forEach(button => {
                if (button.dataset.theme === activeTheme) {
                    button.classList.add('active');
                } else {
                    button.classList.remove('active');
                }
            });
        }

        function loadSettings() {
            const savedTheme = localStorage.getItem('selectedTheme') || 'default';
            applyTheme(savedTheme);

            const savedAnimations = localStorage.getItem('animationsEnabled');
            if (savedAnimations === 'false') {
                document.body.classList.add('no-animations');
                animationToggle.checked = true;
            } else {
                document.body.classList.remove('no-animations');
                animationToggle.checked = false;
            }

            const savedLanguage = localStorage.getItem('selectedLanguage') || 'tr';
            languageSelect.value = savedLanguage;
            // In a real app, you'd load translations here
        }

        function showSettingsPanel() {
            hideProfileDrawer(); // Close profile drawer if open
            settingsPanelOverlay.classList.add('active');
        }

        function hideSettingsPanel() {
            settingsPanelOverlay.classList.remove('active');
        }

        drawerSettings.addEventListener('click', (e) => {
            e.preventDefault();
            showSettingsPanel();
        });

        closeSettingsPanelButton.addEventListener('click', hideSettingsPanel);
        settingsPanelOverlay.addEventListener('click', (e) => {
            if (e.target === settingsPanelOverlay) {
                hideSettingsPanel();
            }
        });

        themeButtons.addEventListener('click', (e) => {
            if (e.target.tagName === 'BUTTON') {
                applyTheme(e.target.dataset.theme);
            }
        });

        animationToggle.addEventListener('change', (e) => {
            if (e.target.checked) {
                document.body.classList.add('no-animations');
                localStorage.setItem('animationsEnabled', 'false');
            } else {
                document.body.classList.remove('no-animations');
                localStorage.setItem('animationsEnabled', 'true');
            }
        });

        languageSelect.addEventListener('change', (e) => {
            localStorage.setItem('selectedLanguage', e.target.value);
            alert(`Dil ${e.target.value === 'tr' ? 'Türkçe' : 'English'} olarak ayarlandı. (Bu sadece bir örnek, gerçek çeviri yapılmadı.)`);
        });

        // Ripple Effect Functionality
        function createRipple(event) {
            const button = event.currentTarget;
            const circle = document.createElement('span');
            const diameter = Math.max(button.clientWidth, button.clientHeight);
            const radius = diameter / 2;

            circle.style.width = circle.style.height = `${diameter}px`;
            circle.style.left = `${event.clientX - button.getBoundingClientRect().left - radius}px`;
            circle.style.top = `${event.clientY - button.getBoundingClientRect().top - radius}px`;
            circle.classList.add('ripple');

            const ripple = button.getElementsByClassName('ripple')[0];

            if (ripple) {
                ripple.remove();
            }

            button.appendChild(circle);
        }

        function addRippleEffect(element) {
            if (element) {
                element.removeEventListener('click', createRipple);
                element.addEventListener('click', createRipple);
            }
        }

        document.querySelectorAll('.google-signin, .cta-button, .contact-panel .contact-item, .close-button, .profile-drawer-item, .profile-drawer-close-button, .logo, .settings-panel .settings-option button').forEach(element => {
            addRippleEffect(element);
        });

        function ensureContactButtonInHeroActions() {
            const heroActionsDiv = heroContentDiv.querySelector('.hero-actions');
            if (!contactAdvisorButton) {
                contactAdvisorButton = document.createElement('button');
                contactAdvisorButton.id = 'contactAdvisorButton';
                contactAdvisorButton.className = 'cta-button secondary';
                contactAdvisorButton.innerHTML = `
                    MÜŞTERİ DANIŞMANINA ULAŞ
                    <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                        <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07 19.5 19.5 0 0 1-6-6 19.79 19.79 0 0 1-3.07-8.63A2 2 0 0 1 4.11 2h3a2 2 0 0 1 2 1.72 12.84 12.84 0 0 0 .7 2.81 2 2 0 0 1-.45 2.11L8.09 9.91a16 16 0 0 0 6 6l1.27-1.27a2 2 0 0 1 2.11-.45 12.84 12.84 0 0 0 2.81.7A2 2 0 0 1 22 16.92z"/>
                    </svg>
                `;
                contactAdvisorButton.addEventListener('click', showContactPanel);
                addRippleEffect(contactAdvisorButton);
            }
            if (!heroActionsDiv.contains(contactAdvisorButton)) {
                heroActionsDiv.appendChild(contactAdvisorButton);
            }
            contactAdvisorButton.style.display = 'inline-flex';
        }

        function removeContactButtonFromHeroActions() {
            if (contactAdvisorButton && contactAdvisorButton.parentNode) {
                contactAdvisorButton.parentNode.removeChild(contactAdvisorButton);
            }
            if (contactAdvisorButton) {
                contactAdvisorButton.style.display = 'none';
            }
        }

        // Scroll Animation (Intersection Observer)
        function initializeScrollAnimations() {
            const animateOnScrollElements = document.querySelectorAll('.scroll-animate');
            const observer = new IntersectionObserver((entries) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add('visible');
                        observer.unobserve(entry.target);
                    }
                });
            }, {
                threshold: 0.1,
                rootMargin: '0px 0px -50px 0px'
            });

            animateOnScrollElements.forEach(element => {
                element.classList.remove('visible');
                observer.observe(element);
            });
        }
        window.addEventListener('load', initializeScrollAnimations);

        // Apply Shimmer to Hero Text
        function applyShimmerToText() {
            const heroTitle = document.querySelector('.hero-title');
            const heroSubtitle = document.querySelector('.hero-subtitle');
            if (heroTitle) heroTitle.style.animation = 'none';
            if (heroSubtitle) heroSubtitle.style.animation = 'none';
            
            void heroTitle.offsetWidth; 
            void heroSubtitle.offsetWidth;

            if (heroTitle) heroTitle.style.animation = '';
            if (heroSubtitle) heroSubtitle.style.animation = '';
        }
        window.addEventListener('load', applyShimmerToText);

        // Company Data for Card Flip
        const companies = [
            {
                logo: "/placeholder.svg?height=80&width=80",
                name: "Tech Solutions A.Ş.",
                industry: "Yazılım Geliştirme",
                reason: "Yenilikçi ve ölçeklenebilir yazılım çözümleri sunarak iş süreçlerimizi optimize ettiler. Hızlı teslimat ve mükemmel destekleri sayesinde projelerimiz zamanında tamamlandı.",
                benefits: ["Ölçeklenebilir Yazılım", "Hızlı Teslimat", "Mükemmel Destek", "İş Süreç Optimizasyonu"]
            },
            {
                logo: "/placeholder.svg?height=80&width=80",
                name: "Global Pazarlama Ltd.",
                industry: "Dijital Pazarlama",
                reason: "Dijital pazarlama stratejilerimizi baştan sona yenilediler. Hedef kitlemize ulaşmamızı sağlayan yaratıcı kampanyalar ve detaylı analizlerle satışlarımızı %40 artırdık.",
                benefits: ["Yaratıcı Kampanyalar", "Detaylı Analiz", "Satış Artışı", "Hedef Kitleye Ulaşım"]
            },
            {
                logo: "/placeholder.svg?height=80&width=80",
                name: "E-Ticaret Devi",
                industry: "Online Perakendecilik",
                reason: "Kullanıcı dostu ve güvenli e-commerce altyapısı sayesinde müşteri deneyimimizi üst seviyeye taşıdık. Entegrasyon süreçleri sorunsuzdu ve teknik ekip her zaman yanımızdaydı.",
                benefits: ["Kullanıcı Dostu Arayüz", "Güvenli Altyapı", "Sorunsuz Entegrasyon", "Müşteri Deneyimi"]
            },
            {
                logo: "/placeholder.svg?height=80&width=80",
                name: "Finansal Çözümler A.Ş.",
                industry: "Finans ve Danışmanlık",
                reason: "Karmaşık finansal verilerimizi anlaşılır raporlara dönüştüren özel bir analiz platformu geliştirdiler. Bu sayede daha bilinçli kararlar alabiliyor ve riskleri minimize edebiliyoruz.",
                benefits: ["Veri Analizi", "Özel Platform", "Bilinçli Kararlar", "Risk Yönetimi"]
            }
        ];

        function createCardFlip(company) {
            const cardContainer = document.createElement('div');
            cardContainer.className = 'card-flip-container scroll-animate';

            const cardInner = document.createElement('div');
            cardInner.className = 'card-flip-inner';

            // Front of the card
            const cardFront = document.createElement('div');
            cardFront.className = 'card-flip-front';
            cardFront.innerHTML = `
                <div class="card-front-content">
                    <img src="${company.logo}" alt="${company.name} Logo" class="card-front-logo">
                    <div class="card-front-text">
                        <h3 class="card-front-title">${company.name}</h3>
                        <p class="card-front-subtitle">${company.industry}</p>
                    </div>
                    <div class="card-front-icon-wrapper">
                        <svg class="card-front-icon" xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 12a9 9 0 1 1-9-9c2.52 0 4.93 1 6.74 2.74L21 12Z"/><path d="M15 6v6h6"/></svg>
                    </div>
                </div>
            `;

            // Back of the card
            const cardBack = document.createElement('div');
            cardBack.className = 'card-flip-back';
            cardBack.innerHTML = `
                <div class="card-back-content">
                    <div class="card-back-text-section">
                        <h3 class="card-back-title">${company.name}</h3>
                        <p class="card-back-description">${company.reason}</p>
                    </div>
                    <div class="card-back-features">
                        ${company.benefits.map(benefit => `
                            <div class="card-back-feature-item">
                                <svg class="card-back-feature-icon" xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m9 18 6-6-6-6"/></svg>
                                <span>${benefit}</span>
                            </div>
                        `).join('')}
                    </div>
                </div>
            `;

            cardInner.appendChild(cardFront);
            cardInner.appendChild(cardBack);
            cardContainer.appendChild(cardInner);

            // Add flip functionality
            cardContainer.addEventListener('mouseenter', () => {
                cardContainer.classList.add('flipped');
            });
            cardContainer.addEventListener('mouseleave', () => {
                cardContainer.classList.remove('flipped');
            });

            return cardContainer;
        }

        // Populate company cards
        window.addEventListener('load', () => {
            const companyCardsGrid = document.getElementById('companyCardsGrid');
            companies.forEach(company => {
                companyCardsGrid.appendChild(createCardFlip(company));
            });
            initializeScrollAnimations();
            loadSettings(); // Load settings on page load
        });
    </script>
</body>
</html>
